# 📊 Прогнозирование отпусков сотрудников  
ML-практика | Ростелеком

---

## 🎯 Цель проекта

Разработать модель машинного обучения для прогнозирования ухода сотрудника в отпуск на основе исторических данных за 2020–2025 годы, а также предсказать на сколько дней будет отпуск. 

Первая задача сформулирована как бинарная классификация:

> `Y = 1`, если сотрудник берёт отпуск в данном месяце.

Дополнительно рассматривалась постановка с forward-прогнозом (отпуск в ближайшие 30/60/90 дней).

---

## 📁 Данные (что я создала для обучения модели)

- 8.2 млн строк
- 31 признак
- ~126 000 сотрудников
- Период наблюдений: 2020–2025

Строка датасета = сотрудник × месяц.

### Группы признаков:

**Исторические**
- `vac_flag_lag1`
- `vac_flag_last3`
- `vac_flag_last6`
- `vac_days_lag1`

**HR-признаки**
- `department`
- `region`
- `is_manager`
- `tenure_months`
- `children_count`
- и др.

**Календарные**
- `month`
- `is_summer_month`

---

## 🧹 Предобработка данных

### 1. Формирование панельной структуры
Создана таблица employee × month.

### 2. Обработка пропусков

| Тип признака | Обработка |
|-------------|-----------|
| Категориальные | fillna("missing") |
| Бинарные | fillna(0) |
| Числовые | медиана train |

Все статистики рассчитывались **только на обучающей выборке** (без утечки данных).

---

### 3. Обнаруженные проблемы

В процессе работы выявлены следующие сложности:

- ❗ 14% отрицательных значений стажа  
  → заменены медианой по возрасту  
  → добавлен флаг `tenure_was_invalid`, чтобы не потерять эти данные

- ❗ Неполные данные до 2022 года  
  → данные 2020–2021 исключены из обучения, так как они могли помешать обучению модели из-за неполноты

- ❗ Структурные пропуски после merge статических признаков

- ❗ Утечка данных через признаки:
  - `vacation_days`
  - `vacations_cnt`
  - `start_min`
  
  Эти признаки были удалены.

- ❗ Важно учитывать, что в данной задаче есть дисбаланс классов (~15% положительного класса)

---

## 🧠 Модель

Использована:

### CatBoostClassifier

Причины выбора:
- устойчивость к категориальным признакам
- корректная работа с пропусками
- высокая эффективность на табличных данных

Параметры:

- флаг отпуска в текущем периоде — целевая переменная (`vacation_flag`)
- количество дней отпуска — целевая переменная для регрессии (`vac_days`)
- индикатор некорректного стажа (`tenure_negative_flag`)

Все лаговые признаки рассчитывались с использованием временного сдвига (`shift(1)`),
что позволило исключить использование информации из будущего периода!

Лаговые признаки формировались группировкой по `employee_id`
с последующим применением временного сдвига по `period_month`.


---

## 📊 Валидация

### 🔹 1. Обычный временной split

Train: 2022–2023  
Validation: 2024  
Test: 2025  

### Результаты:

- ROC-AUC: **0.768**
- PR-AUC: 0.378
- Precision: 0.32
- Recall: 0.69
- F1: 0.44

---

### 📈 ROC Curve (без time-folds)
Тут мы можем увидеть довольно высокий ROC-AUC, но для честности я решила обучить еще на фолдах.
![insert_image](https://github.com/aatoian/vacations_rostelecom/blob/main/Снимок%20экрана%202026-02-15%20в%2000.31.44.jpg?raw=true)

---

## 🔄 Time-Based Cross Validation

Реализована rolling-временная валидация:

- Окно валидации: 3 месяца (добавила новые колонки с лагами)
- Обучение только на прошлом
- Без утечки будущей информации

### Проблема

На ранних фолдах качество существенно снижалось (ROC-AUC ≈ 0.57),  
что связано с недостаточным объёмом исторических данных.

После ограничения минимального размера train-окна качество стабилизировалось.

---

### 📈 ROC Curve (Time-Based CV)
![insert_image](https://github.com/aatoian/vacations_rostelecom/blob/main/Снимок%20экрана%202026-02-15%20в%2001.32.31.jpg?raw=true)

---

## 📉 Почему метрика снизилась при CV?

Time-based CV является более строгим методом оценки.

Причины падения ROC:

- модель обучалась на меньшем объёме истории
- присутствует сезонность
- возможный data drift

Это свидетельствует о честной оценке модели.

---

## 📌 Выводы

- ROC-AUC ~0.75 для HR-поведенческой задачи является хорошим baseline.
- Модель чувствительна к объёму исторических данных.
- Временная кросс-валидация даёт более реалистичную оценку.
- Основные влияющие признаки:
  - год
  - регион
  - возраст
  - история отпусков

---

## 💻 Ограничения инфраструктуры

В процессе обучения возникли ограничения по оперативной памяти (RAM ~12GB).

Полный датасет содержит более 8 млн строк, поэтому:

- Обучение на всём объёме данных приводило к исчерпанию памяти.
- Time-Based Cross Validation дополнительно увеличивала нагрузку (модель обучается несколько раз).
- Для обеспечения стабильности использовалось обучение на сэмплах (600 000 – 1 000 000 строк).

Таким образом, итоговые метрики CV получены на уменьшенных выборках.

---

## 🚀 Возможные улучшения

1. Обучение на полном объёме данных с использованием:
   - GPU-ускорения (CatBoost GPU)
   - серверной инфраструктуры с большим объёмом RAM

2. Тонкая настройка гиперпараметров (Optuna / GridSearch)

3. Использование forward-таргета (30/60/90 дней)

4. Оптимизация порога классификации под бизнес-метрику

5. Добавление rolling-агрегатов и сезонных признаков

6. Анализ data drift между 2022–2025

7. Использование более продвинутых ансамблей моделей

