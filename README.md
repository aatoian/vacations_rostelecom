# vacations_rostelecom
# 📊 Прогнозирование отпускной активности сотрудников

## 🔎 Контекст проекта

Проект выполнен в рамках стажировки и направлен на разработку моделей машинного обучения
для прогнозирования отпускного поведения сотрудников на основе исторических HR-данных.

Задача имеет временную структуру и требует строгого контроля утечек данных (data leakage).
## 📂 Данные

В работе использовались агрегированные HR-данные по сотрудникам
(панельный формат: сотрудник × период):

- идентификатор сотрудника (`employee_id`)
- период наблюдения, месяц (`period_month`)
- флаг отпуска в текущем периоде (`vacation_flag`)
- количество дней отпуска (`vac_days`)
- стаж сотрудника (`tenure`)
- индикатор некорректного стажа (`tenure_negative_flag`)

Данные имели временную структуру и позволяли анализировать
динамику отпускной активности сотрудников.
## 🎯 Постановка задач

В рамках проекта решались две задачи:

1. Классификация — прогноз факта ухода в отпуск в период t
2. Регрессия — прогноз количества дней отпуска в период t

Все признаки формировались исключительно на основе информации,
доступной до момента прогнозирования.

## 🧩 Feature Engineering

Для выявления поведенческих паттернов были созданы лаговые признаки:

- отпуск в предыдущем периоде (`vac_flag_lag1`)
- длительность отпуска в предыдущем периоде (месяце) (`vac_days_lag1`)
- количество отпусков за последние 3 периода (`vac_flag_last3`)
- количество отпусков за последние 6 периодов  (`vac_flag_last6`)

Дополнительно использовались:

- флаг отпуска в текущем периоде — целевая переменная (`vacation_flag`)
- количество дней отпуска — целевая переменная для регрессии (`vac_days`)
- индикатор некорректного стажа (`tenure_negative_flag`)

Все лаговые признаки рассчитывались с использованием временного сдвига (`shift(1)`),
что позволило исключить использование информации из будущего периода!

Лаговые признаки формировались группировкой по `employee_id`
с последующим применением временного сдвига по `period_month`.

## 🚨Проблемы, с которыми столкнулась

Было выявлено:
- агрегированные признаки рассчитывались без временного сдвига, что приводило к плохому качеству модели.
- использовалось некорректное разделение train/test (AUC=0.62)
- модель получала информацию из будущего периода (я забыла удалить колонку с кол-вом отпусков у сотрдуника)

После устранения утечки:
- лаги пересчитаны с использованием shift(1)
- реализована временная валидация 
- пересобран pipeline

Метрика стала реалистичной.

## 📈 Эволюция модели

Baseline модель → ROC AUC = 0.62  
Добавление лагов → ROC AUC = 0.70  
Leakage (ошибка) → ROC AUC > 0.99  
Исправление → ROC AUC = 0.73
Финальная модель → ROC AUC = 0.78 

## 📐 Регрессионная модель

Дополнительно построена модель для прогнозирования длительности отпуска.

Используемые метрики:
- MAE
- RMSE


## ⚙ Основные сложности
Основная сложность — сильная асимметрия распределения и наличие нулевых значений. А также
- Дисбаланс классов
- Временная зависимость данных
- Утечка данных
- Выбор корректной стратегии валидации
- Интерпретация метрик

![image_alt]https://github.com/aatoian/vacations_rostelecom/blob/2d30fe4f31ddc73b44da408ddc69b601d8db307b/%D0%A1%D0%BD%D0%B8%D0%BC%D0%BE%D0%BA%20%D1%8D%D0%BA%D1%80%D0%B0%D0%BD%D0%B0%202026-02-15%20%D0%B2%2001.32.31.png
## 📚 Ключевые выводы

- Историческое поведение сотрудника является главным предиктором.
- В задачах с временными данными критична корректная валидация.
- Data leakage может радикально искажать результаты.
- Последовательная инженерная работа повышает устойчивость модели.

